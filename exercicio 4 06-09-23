Algoritmo "desconto"
// Disciplina   : [Operacoes matematicas]
// Professor   : Marcelo
// Descrição   : Aqui você descreve o que o programa faz! (função)
// Autor(a)    : Vitor Henrique de Jesus
// Data atual  : 06/09/2023
Var
// Seção de Declarações das variáveis 

produto: caracter
valor, porcentagem: real
val_desconto: real
val_final: real

Inicio
escreval ("Digite o nome do produto")
leia (produto)
escreval ("Digite o valor do produto")
leia (valor)
escreval ("Digite a % do desconto")
leia (porcentagem)

val_desconto <- (porcentagem*valor)/100

val_final <- (valor-val_desconto)

escreval("O produto"," ",produto," ","custa R$"," ",valor," ","mas você tem um desconto de"," ",porcentagem,"%")

escreval("por este motivo, será abatido em sua compra o valor de R$",val_desconto)

escreval("desta forma, você pagará apenas R$", val_final," ","pelo produto")



Fimalgoritmo
