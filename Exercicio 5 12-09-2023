Algoritmo "Testar Notas"
// Disciplina   : Fluxogramas e condicionais]
// Professor   : Marcelo
// Descrição   : Aqui você descreve o que o programa faz! (função)
// Autor(a)    : Vitor Henrique de Jesus
// Data atual  : 12/09/2023
Var
nome: caracter
disciplina: caracter
nota: real


Inicio
escreval("Digite o seu nome")
leia (nome)
escreval ("Digite a disciplina")
leia (disciplina)
escreval ("Digite o valor de sua nota")
leia (nota)

se (nota <= 0) ou (nota  > 10) entao
    escreval ("Nota invalída")
  senao
     se (nota >= 7) e (nota <10) entao
       escreval ("O aluno está aprovado")
    senao
       se (nota >= 5) e (nota < 7) entao
           escreval ("o aluno está de 2a chamda")
      senao
         se (nota >= 3) e (nota < 5) entao
             escreval (" o aluno esta em recuperação")
        senao
           se (nota > 0) e (nota < 3) entao
               escreval (" o aluno esta reprovado")
           fimse
          fimse
         fimse
       fimse
fimse
fimalgoritmo
